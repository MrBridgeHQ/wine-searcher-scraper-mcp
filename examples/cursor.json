{
  "_comment": "Add this entry to your Cursor MCP config: .cursor/mcp.json in your project root, OR ~/.cursor/mcp.json for global config.",
  "mcpServers": {
    "wine-searcher": {
      "command": "npx",
      "args": [
        "-y",
        "@apify/actors-mcp-server",
        "--actors",
        "mrbridge/wine-searcher-scraper-from-list"
      ],
      "env": {
        "APIFY_TOKEN": "YOUR_APIFY_TOKEN_HERE"
      }
    }
  }
}
